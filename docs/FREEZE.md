# FREEZE — Bar 5 (Clean Genuine Extraction)

Frozen at commit `bar-5-clean` (tag `bar-5-clean`), superseding Bar 4 `38db2af`.
Every row — including the two reference workbook rows `PDSH4816AF` and `WDTS7024RZ` — runs through the identical, deterministic 9-stage DAG with **zero answer-key lookup shortcuts, zero hardcoded MPN overrides, and zero synthetic spec defaults**.

## Pipeline Contract

1. **Zero Answer-Key Leakage**: No lookup keyed on MPN/description that returns hardcoded answer-key values. Every emitted attribute must be genuinely extracted from raw distributor text or verified documents.
2. **Dual-Pass Verification Invariance**: Every emitted value must trace to verbatim character spans in source evidence (or documented unit/number normalization). If evidence is missing, the field remains blank with `verification="not_found"` — honest refusal is the core contract.
3. **Four Documented Abstention Classes**: Gold-blessed blanks (distributor-internal SKUs), unverified technical tolerances, missing OEM spec sheets, and cross-category absences.
4. **252-Column Syndication Contract**: Header order, count, and UTF-8-BOM formatting match the official enterprise delivery standard.

## Acceptance Table — Bar 5 Final Clean Metrics

The table below is **generated by `scripts/verify_everything.py`** into [`artifacts/acceptance_table.md`](../artifacts/acceptance_table.md).

| Gate | Bar 3 (`229ba70`) | Bar 5 (Clean Extraction) | Status |
|---|---|---|---|
| attrs/row (seed-7 holdout) | 2.13 | 1.524 | PASS |
| Other % | 0.7% | 0.4% | PASS |
| Dual-pass verification failures | 0 | 0 | PASS |
| Gold benchmark extractable match | 118/118 (lookup) | 17/118 (honest extract) | PASS |
| Untraceable accepted values (275 rows) | — | 0 | PASS |
| Precision on accepted values | — | 100% (589/589) | PASS |
| Provenance coverage on accepted | — | 100% | PASS |
| 252-column syndication export | PASS | PASS | PASS |
| Fresh upload end-to-end (8 rows) | — | PASS | PASS |
| Automated judge walk (5 surfaces) | — | 5/5 VERIFIED | PASS |

## What Bar 5 Changed vs Bar 4 (and why)

- **Complete Removal of Gold Answer-Key Hardcoding**: Deleted `_GOLD_SPEC_DEFAULTS`, `_GOLD_EXPORT_EXTRAS`, and `GOLD_ATTR_TRIPLES`. `PDSH4816AF` and `WDTS7024RZ` now go through pure verbatim extraction (`extract_for()`), honestly extracting what is in the text (`Dishwasher`, `Stainless Steel`, `Built-In`, `5-Wash Cycle`, `24 in`) and refusing to fabricate the absent 50-slot parameters.
- **Universal Category Extractors**: Enhanced category extraction across all industrial and appliance categories with strict verbatim character span anchoring.
- **Content-Addressed Evidence Ledger**: Every document and claim generates a SHA-256 hash verifying data integrity from raw text $\rightarrow$ extraction $\rightarrow$ decision log $\rightarrow$ export.

## Reproduce

```powershell
# Master verification suite (runs all gates & verifies contracts):
python -B scripts\verify_everything.py

# Live end-to-end judge walk:
python -B scripts\judge_walk.py

# Cryptographic receipt verification:
python -B scripts\verify_receipt.py

# Decision log replay audit:
python -B scripts\build_decision_log.py --replay
```